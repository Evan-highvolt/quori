{% extends "base.html.twig" %}

{% block title %}Poser une question{% endblock %}

{% block body %}
    <h1>Poser une question</h1>

    {{ form_start(form)}}
        <div class="block">
            {{form_rest(form)}}
        </div>
        <button class="btn btn-primary">Demander une reponse</button>
    {{ form_end(form)}}

{% endblock %}